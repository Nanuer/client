<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <RelativeLayout
        android:id="@+id/boss_header_rl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="20dp"
        android:background="@drawable/textline_bottom_gray">
        <TextView
            android:id="@+id/chat_personal_title_tv"
            style="@style/TitleBig"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginStart="32dp"
            android:includeFontPadding="false"
            android:text="결제 확인"
            android:textColor="@color/main_black" />
    </RelativeLayout>

    <TextView
        android:id="@+id/boss_tv"
        style="@style/TitleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="방 구성원들이 돈을 보내는 중입니다.\n 모두 확인이 되면 결제 확인 버튼을 눌러주세요."
        android:gravity="center"
        android:textColor="@color/black"
        android:includeFontPadding="false"
        app:layout_constraintTop_toBottomOf="@id/boss_header_rl"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:layout_marginTop="70dp"
        />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/boss_ok_btn"
        style="@style/BtnMid"
        android:layout_width="200dp"
        android:layout_height="45.3dp"
        android:text="결제 확인"
        android:textColor="@color/white"
        android:background="@drawable/blue_button_on"
        android:gravity="center"
        android:includeFontPadding="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/boss_tv"
        android:layout_marginTop="60dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>